#include <iostream>

#include "scanner.h"

void Scanner::lex_file(Filestream *input) {
	// char output[100];
	// if (input->is_open()) {
	// 	while (!input->eof()) {
	// 		std::cout << input;
	// 	}
	// }
}
